Plan 1: Implement `EthnicityBiasProber` in its own `prober.py`, force all logging through `aim.sdk.agent.research_agent_logger`, and ensure every reused baseline test metric carries the `agent_log_test_` prefix while new probe metrics use the `agent_log_prober_` prefix. Steps: 1) Load eval set plus model checkpoints, split samples by the six ethnicity one-hot bins exposed in `dataset.py`. 2) For each group compute AUC, PR-AUC, recall@0.5, and Brier score using shared metric helpers, wrapping computations in small reusable functions for clarity. 3) Push baseline global metrics (for comparison) with `agent_log_test_` names, then log per-group probe metrics like `agent_log_prober_auc_ethnicity_0` via the tracking API. 4) Aggregate a table-like dict plus optional Matplotlib bar plot (logged through the same logger) to highlight disparities. 5) Expose a `run(model, eval_loader)` entrypoint returning structured probe results so the training harness can invoke it post-training.