Plan 1: Implement `prober.py` that reloads the fine-tuned checkpoint and test loader from `dataset.py`, runs baseline inference per sample, then clones each sample six times swapping only the 6-D ethnicity vector to measure probability shifts; aggregate per-class stats (mean/variance/max shift), log swap-level distributions and a histogram artifact, and record supporting test-run stats via `aim.sdk.agent.research_agent_logger` with metric names prefixed `agent_log_test_` for generic evaluation outputs and `agent_log_prober_` for the counterfactual sensitivity metrics so the research agent captures them.