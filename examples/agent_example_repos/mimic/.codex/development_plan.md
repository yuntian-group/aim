Plan 1 – Direct AUROC threshold/delta detection: ensure evaluate() logs the validation metric via aim.sdk.agent.research_agent_logger ResearchAgentLogger using the required test prefix agent_log_test_, e.g., agent_log_test_val_auroc, so the downstream prober has a canonical signal; implement ValidationAUROCThresholdProber inside prober.py that reuses evaluate() to compute current_auroc and loads the most recent test metric value (cached locally if the tracking API cannot query history) to compute the numeric conclusion score = min(current_auroc - 0.78, current_auroc - last_auroc); log score plus a binary flag through the same tracking API under agent_log_prober_auroc_plateau_score and agent_log_prober_auroc_plateau_flag so the research agent captures them; use threshold -0.02 (score ≥ -0.02 means good, score < -0.02 means AUROC plateau/bad) and state this in the summary; always render a Matplotlib line plot of historical AUROC vs run index with the 0.78 horizontal reference, fixing the x-axis to 0-20 runs and the y-axis to 0.5-1.0 every time, and log that figure through ResearchAgentLogger.