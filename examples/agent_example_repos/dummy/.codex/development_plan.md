Plan 1 – Deterministic replicate drift: implement a Prober class in prober.py that shells out `python train.py --epochs=5 --seed=0` twice back-to-back, parses validation loss per epoch, and streams every run-level metric via aim.sdk.agent.research_agent_logger; all run metrics must use the agent_log_test_ prefix while the prober-level conclusion/threshold use agent_log_prober_; compute the numerical conclusion `abs(val_loss_run2_final - val_loss_run1_final)` logged as `agent_log_prober_loss_drift_final`, mark it bad when >0.02 and good otherwise, and log the 0.02 threshold beside it; capture a dual-line Matplotlib figure (epochs fixed to 1..5 on x, losses fixed to [0,1] on y for every run) plus a short textual summary, and log both artifacts through the same tracking API so the research agent ingests the quantified drift outcome.